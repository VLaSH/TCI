<%= render(:partial => 'attachments/new_and_edit_form', :locals => { :attachment => @attachment, :path => instructor_attachment_path(@attachment, params.merge(submission_id: params[:submission_id])) }) -%>
