<div class="grid_16 margin_top_20 alpha omega">
  <h6>Delete blog</h6>
  <table class="pretty">
    <thead>
      <tr>
        <th class="centred">Title</th>
        <th class="centred">Content</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= h(@blog.title) %></td>
        <td><%= @blog.content.html_safe %></td>
      </tr>
    </tbody>
  </table>

  <%= form_for([ :administrator, @blog ], :html => { :method => :delete }) do |f| %>
    <div class="clear">&nbsp;</div><br />
    <p class="buttons"><%= pretty_button "Delete", {:icon_path => "icons/delete.png"} %>
  <% end %>
</div>
