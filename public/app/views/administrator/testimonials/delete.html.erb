<div class="grid_16 margin_top_20 alpha omega">
  <h6>Delete Testimonial <%= h(@testimonial.title) -%></h6>
  <table class="pretty">
    <thead>
      <tr>
        <th class="centred">Title</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= h(@testimonial.title) %></td>
      </tr>
    </tbody>
  </table>

  <%= form_for([ :administrator, @testimonial ], :html => { :method => :delete }) do |f| %>
    <div class="clear">&nbsp;</div><br />
    <p class="buttons"><%= pretty_button "Delete", {:icon_path => "icons/delete.png"} %>
  <% end %>
</div>
